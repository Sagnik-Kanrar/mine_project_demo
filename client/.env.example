# Development API URL (override if backend is on different host/port)
# VITE_API_URL=http://localhost:5000/api

# Development WebSocket URL (override if backend is on different host/port)
# VITE_WS_URL=ws://localhost:5000/ws

# Build environment
# VITE_ENV=development
